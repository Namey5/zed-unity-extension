{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "monoPath",
    "port"
  ],
  "properties": {
    "monoPath": {
      "type": "string",
      "description": "Path to the Mono C# runtime executable (eg. `mono.exe` on Windows) - this will default to the MONO_PATH environment variable (if set)."
    },
    "logLevel": {
      "type": "string",
      "enum": [
        "trace",
        "debug",
        "info",
        "warn",
        "error",
        "critical",
        "none"
      ],
      "default": "warn"
    },
    "address": {
      "type": "string",
      "description": "IPv4 address of the running Unity instance to debug.",
      "default": "127.0.0.1"
    },
    "port": {
      "type": "number",
      "description": "Open debugging port that Unity is listening on - this will be automatically determined from the process id (if provided)."
    }
  }
}
